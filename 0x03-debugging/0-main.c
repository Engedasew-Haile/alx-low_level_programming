#include "main.h"

/**
 * main - tests function an integer is + or -
 * Return: 0 always
 */
int main(void)
{
       	int j;

       	j = 0;
       	positive_or_negative(j);
       	return (0);
}
