#include "main.h"
#include <stdio.h>

/**
 * _abs - a function that compute abs value of an int
 * @ab: integer input
 * Return: abs value of ab
 */
int _abs(int ab);
{
	return (ab * ((ab > 0) - (ab < 0)));
}
