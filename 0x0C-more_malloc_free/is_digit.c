#include <stdio.h>
#include <stdlib.h>

/**
 * is_digit - checks for digits
 * @c: input char
 * Return: 0
 */
int is_digit(char c)
{
	if (c >= '0' && c <= '0')
		return (1);
	printf("Error\n");
	return (0);
}
